<Window x:Class="DesktopUI.Views.MessageBoxes.LoginErrorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopUI.Views.MessageBoxes"
        mc:Ignorable="d"
        Title="Error" Height="135" Width="300" 
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" 
        AllowsTransparency="True">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="..\..\Images\checkbox_red.jpg"/>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="40"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="1"
                   Text="Ugyldigt Login!"
                   TextWrapping="Wrap"
                   FontSize="20" FontFamily="Constantia" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Button Grid.Column="2"
                x:Name="CloseWindow" 
                Style="{StaticResource CloseBtnStyle}"/>

    </Grid>
</Window>
