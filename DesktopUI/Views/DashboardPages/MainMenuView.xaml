<UserControl x:Class="DesktopUI.Views.DashboardPages.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopUI.Views.DashboardPages"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="750">

    <Grid>
        <Label x:Name="FullName" 
               Margin="15"
               FontSize="40"
               VerticalAlignment="Top"
               Foreground="{StaticResource White}"
               Style="{StaticResource HeaderLabel}"
               ContentStringFormat="Velkommen {0}"/>

        <Button x:Name="LogOut"
                Content="Log Ud"
                Margin="0 0 0 15"
                VerticalAlignment="Bottom" HorizontalAlignment="Center"
                Style="{StaticResource RedButton}"/>
    </Grid>
</UserControl>
